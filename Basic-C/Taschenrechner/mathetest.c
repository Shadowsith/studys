#include <stdio.h>
#include <math.h> // Mathematik Bibliothek einbinden.

int main()
{
    int a = 2;
    int b = 2;
    int result = pow(a,b);
    printf("Das Quadrat von 2 ist %d.\n", result);
    return 0;
}
